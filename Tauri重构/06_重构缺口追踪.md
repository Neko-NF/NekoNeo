# NekoNeo 重构缺口追踪

> 日期：2026-05-10

## P0
- [x] 启动服务前的服务器与设备密钥校验
- [x] 密钥冲突、撤销、设备不存在的启动与运行时处理
- [x] 真实系统指标、窗口枚举、电池、字体采集
- [x] 最小 HTTP 状态上报链路
- [x] 截图链路第一阶段：真实截图采集、本地落盘、隐私模糊、tick 接入
- [x] 截图上传到服务端
- [x] 最近截图元数据查询接口
- [x] 截图活动页与预览链路
- [x] 看门狗自动恢复完整实现
- [x] 开机自启真实接入

## P1
- [x] 隐私规则列表管理与命中脱敏
- [x] 隐私可视化选择器覆盖层
- [x] 系统托盘与关闭行为联动
- [x] 系统通知与勿扰联动
- [x] 自动更新真实检查、下载、安装
- [x] 连通性测试与设备元信息同步

## P2
- [x] SMTC 媒体信息
- [x] 空闲检测精细化
- [x] 应用图标采集
- [x] 设备指纹完整接入
- [x] 单例运行锁
- [x] 认证、自动生成设备密钥、账户侧个人信息流程

## 当前结论
- 当前重构版已经具备”配置 -> 启动校验 -> 定时上报 -> 窗口隐私脱敏 -> 截图采集/上传 -> 最近截图预览”的最小闭环。
- P0/P1/P2 所有缺口均已完成，项目具备替代原项目的完整核心能力。

## 2026-05-10 P2 增量完成
- [x] 已完成：`SystemInfo::get_idle_ms()` 使用 `GetLastInputInfo` Win32 API 精确检测用户空闲时长，替换原有粗糙的 CPU < 8% 启发式判断
- [x] 已完成：`SystemInfo::get_media_info()` 通过 Windows SMTC (`GlobalSystemMediaTransportControlsSessionManager`) 获取当前播放媒体信息（标题/艺术家/专辑）
- [x] 已完成：`SystemInfo::get_process_icon()` 使用 `ExtractIconExW` + GDI 从 exe 提取图标并编码为 PNG
- [x] 已完成：`device_fingerprint.rs` 服务综合 CPU/MAC/MachineGuid 生成 SHA-256 设备指纹
- [x] 已完成：`tauri-plugin-single-instance` 插件接入，第二个实例自动聚焦已有窗口
- [x] 已完成：`ApiClient` 新增 `auth_register` / `auth_login` / `auth_get_me` / `auth_generate_device_key` 认证方法，`commands::auth` 模块提供对应 4 个 Command
- [x] 已完成：`Settings.vue` 新增「账户」分区，支持登录/注册/自动生成设备密钥
- [x] 已完成：前端类型 (`MediaInfo`, `DeviceKeyResponse`, `AuthResponse` 等) 和 commands 封装同步更新
- [x] 已完成：`TickResult` 新增 `idleMs` 字段，前后端类型同步
- [x] 已完成：`cargo check` / `cargo test` (14 tests) / `vite build` 全部通过

## 2026-05-10 增量完成
- [x] 已完成：最近截图状态查询、手动截图、Dashboard 最近截图预览链路
- [x] 已完成：连续失败阈值、看门狗超时、限次自动恢复与服务状态联动
- [x] 已完成：Windows 开机自启注册表同步、最小化启动参数、启动延时参数接入
- [x] 已完成：Privacy 可视化选择器覆盖层、托盘与关闭行为联动、通知/DND、真实更新链路、连通性测试与设备元信息同步
